{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Run Web",
        "type": "chrome",
        "request": "launch",
        "runtimeExecutable": "/usr/bin/brave",
        "url": "http://localhost:3000",
        "webRoot": "${workspaceFolder}"
      }
    ]
  }